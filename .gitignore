# Secrets and local config
.env
.env.*
!.env.example
!.env*.example

# Keys and credential dumps
*.pem
*.key
*.p12
*.pfx
credentials.json
credentials*.json
secrets.json
results.json
*_result.json
# Repo-root JSON is always an SP/secrets dump. Package JSON lives under frontend/.
/*.json
*_kimi_*.json
*_kimi_result.json
new_final.json
cursor-cli-monitor-setup-prompt.md
*.sql.gz
*.dump

# Member/endpoint mapping sheet (names + Azure resource hosts). Local only.
*.csv
!*.csv.example
!**/*.csv.example
imp_data.csv
**/imp_data.csv

# Local Azure CLI config if copied into the tree
.azure/
az-submit/

# Google service-account key (Sheets inventory)
secrets/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
dist/
frontend/.vite/
frontend/.env
frontend/.env.*
!frontend/.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
